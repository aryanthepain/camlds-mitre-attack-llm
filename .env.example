# Optional: only needed if you modify the code to use external hosted LLM APIs.
# This project runs with a retrieval baseline by default and supports local Ollama.
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen2.5:7b-instruct
